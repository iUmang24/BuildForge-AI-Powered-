version: "3.8"
services:
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: test_buildforge
    ports:
      - "3306:3306"
    volumes:
      - ./backend/database/dump:/docker-entrypoint-initdb.d
